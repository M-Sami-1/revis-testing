# worst python code ever written

Change `import maths` to `import math`.

Separate function parameters with a comma: `def add_numbers(a, b):`.
Indent the `print` statement by four spaces: `    print("Adding numbers")`.
    return a + b + c   # c is undefined

x = input("Enter number: ")   # string input not converted
y == 10   # comparison instead of assignment

if x = y:   # assignment inside condition (syntax error)
    print("Equal"
else:
    print("Not Equal"))

for i in range(10)
    print(i)   # missing colon above

list = [1,2,3]
print(list[5])   # index out of range

dict = {"a":1, "b":2}
print(dict["c"])   # key error

while True
    breakk   # typo in keyword

class MyClass
    def __init__(self):
        self.value = 10

obj = MyClass()
print(obj.val)   # wrong attribute name

def func():
    return
    print("This will never run")

func(10)   # function takes no arguments

open("file.txt", "r")   # file may not exist, no handling

print(unknown_var)   # completely undefined variable
