#include <stdio.h>
Change the `main` function signature to `int main() {`.
Replace `print("hello")` with `printf("hello\n");` to use the standard library function and include a newline.
Add `return 0;` before the closing brace of the `main` function.
